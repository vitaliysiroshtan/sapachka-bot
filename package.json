{
  "name": "sapachka-bot",
  "version": "1.0.0",
  "description": "Telegram bot that silently removes duplicate messages within a configurable time window",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "dotenv": "^16.0.0",
    "grammy": "^1.36.0"
  }
}
